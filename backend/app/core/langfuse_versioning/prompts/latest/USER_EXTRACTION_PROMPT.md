## Requirement Context

{{context_requirement}}

## Document Content

{{combined_text}}

## Additional Notes

{{additional_context}}

Based on the content above, design appropriate entity types and relation types.

**Rules you must follow**:
1. You must output exactly 10 entity types.
2. The final 2 must be fallback types: `Person` and `Organization`.
3. The first 8 must be concrete types derived from the text.
4. All entity types must be real-world actors that can speak or act in social media contexts, not abstract concepts.
5. Attribute names must not use reserved fields such as `name`, `uuid`, or `group_id`; use alternatives like `full_name` or `org_name`.

